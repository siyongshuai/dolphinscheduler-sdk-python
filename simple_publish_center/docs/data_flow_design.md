# 数据流设计文档

## 目录
1. [概述](#概述)
2. [数据流组件](#数据流组件)
3. [数据流图](#数据流图)
4. [数据结构](#数据结构)
5. [数据转换](#数据转换)
6. [数据存储](#数据存储)
7. [错误处理](#错误处理)

## 概述

本文档描述了工作流管理系统中的数据流设计，包括系统各组件之间的数据交互、数据结构定义以及数据处理流程。该设计旨在确保系统中的数据流动清晰、高效，同时保持数据的一致性和完整性。

## 数据流组件

系统中的主要数据流组件包括：

1. **前端界面**：用户交互的入口，负责数据的展示和收集
2. **API服务**：处理前端请求，进行业务逻辑处理
3. **工作流引擎**：解析和执行工作流定义
4. **脚本执行器**：执行用户定义的脚本
5. **数据库**：持久化存储系统数据
6. **缓存服务**：临时存储频繁访问的数据
7. **消息队列**：处理异步任务和事件通知
8. **文件存储**：存储工作流定义文件、脚本文件等

## 数据流图

### 整体数据流图

```
+-------------+     +-------------+     +----------------+
|  前端界面   |---->|  API服务    |---->|  工作流引擎    |
+-------------+     +-------------+     +----------------+
       |                  |                    |
       |                  v                    v
       |           +-------------+     +----------------+
       |           |  数据库     |<--->|  脚本执行器    |
       |           +-------------+     +----------------+
       |                  ^                    ^
       |                  |                    |
       v                  v                    v
+-------------+     +-------------+     +----------------+
|  缓存服务   |<--->|  消息队列   |<--->|  文件存储      |
+-------------+     +-------------+     +----------------+
```

### 工作流创建数据流

```
+-------------+     +-------------+     +----------------+
|  用户       |---->|  前端界面   |---->|  API服务       |
+-------------+     +-------------+     +----------------+
                          |                    |
                          v                    v
                    +-------------+     +----------------+
                    |  验证工作流 |---->|  保存到数据库  |
                    +-------------+     +----------------+
                                               |
                                               v
                                        +----------------+
                                        |  生成工作流ID  |
                                        +----------------+
                                               |
                                               v
                                        +----------------+
                                        |  返回结果      |
                                        +----------------+
```

### 工作流执行数据流

```
+-------------+     +-------------+     +----------------+
|  用户/调度器|---->|  API服务    |---->|  工作流引擎    |
+-------------+     +-------------+     +----------------+
                                               |
                                               v
                                        +----------------+
                                        |  解析工作流    |
                                        +----------------+
                                               |
                                               v
                                        +----------------+
                                        |  执行任务节点  |
                                        +----------------+
                                               |
                                               v
+-------------+     +-------------+     +----------------+
|  更新状态   |<----|  记录日志   |<----|  处理执行结果  |
+-------------+     +-------------+     +----------------+
```

## 数据结构

### 工作流定义

```json
{
  "workflow_id": "string",
  "name": "string",
  "description": "string",
  "version": "number",
  "create_time": "datetime",
  "update_time": "datetime",
  "status": "string",
  "nodes": [
    {
      "node_id": "string",
      "name": "string",
      "type": "string",
      "params": {
        "key": "value"
      },
      "position": {
        "x": "number",
        "y": "number"
      }
    }
  ],
  "edges": [
    {
      "edge_id": "string",
      "source_node_id": "string",
      "target_node_id": "string",
      "condition": "string"
    }
  ]
}
```

### 任务执行实例

```json
{
  "instance_id": "string",
  "workflow_id": "string",
  "workflow_name": "string",
  "start_time": "datetime",
  "end_time": "datetime",
  "status": "string",
  "node_instances": [
    {
      "node_instance_id": "string",
      "node_id": "string",
      "name": "string",
      "start_time": "datetime",
      "end_time": "datetime",
      "status": "string",
      "params": {
        "key": "value"
      },
      "result": {
        "key": "value"
      },
      "error_message": "string"
    }
  ]
}
```

### 脚本定义

```json
{
  "script_id": "string",
  "name": "string",
  "description": "string",
  "type": "string",
  "content": "string",
  "version": "number",
  "create_time": "datetime",
  "update_time": "datetime",
  "params": [
    {
      "name": "string",
      "type": "string",
      "default_value": "string",
      "required": "boolean",
      "description": "string"
    }
  ]
}
```

## 数据转换

### 前端到API服务

1. **表单数据转换**
   - 用户在前端填写的表单数据被转换为JSON格式
   - 前端进行基本的数据验证（必填项、格式检查等）
   - 数据通过HTTP请求发送到API服务

2. **工作流设计转换**
   - 前端绘制的工作流图被转换为标准工作流定义格式
   - 节点坐标和连线信息被保留用于前端显示
   - 前端验证工作流的基本有效性（如无悬空节点）

### API服务到工作流引擎

1. **工作流定义转换**
   - API服务将用户提交的工作流定义转换为工作流引擎可执行的格式
   - 补充默认参数和系统参数
   - 验证工作流的结构和逻辑正确性

2. **参数映射**
   - 将输入参数映射到各个节点需要的参数
   - 处理参数依赖关系（某节点输出作为另一节点输入）
   - 替换环境变量和系统变量

## 数据存储

### 数据库表设计

1. **工作流表（workflows）**
   - workflow_id: 主键
   - name: 工作流名称
   - description: 描述
   - version: 版本号
   - definition: 工作流完整定义（JSON）
   - status: 状态（草稿、已部署等）
   - create_time: 创建时间
   - update_time: 更新时间
   - creator_id: 创建者ID

2. **工作流实例表（workflow_instances）**
   - instance_id: 主键
   - workflow_id: 关联工作流ID
   - start_time: 开始时间
   - end_time: 结束时间
   - status: 状态（运行中、成功、失败等）
   - params: 执行参数（JSON）
   - result: 执行结果（JSON）
   - error_message: 错误信息

3. **节点实例表（node_instances）**
   - node_instance_id: 主键
   - instance_id: 关联工作流实例ID
   - node_id: 节点ID
   - start_time: 开始时间
   - end_time: 结束时间
   - status: 状态
   - params: 执行参数（JSON）
   - result: 执行结果（JSON）
   - error_message: 错误信息

4. **脚本表（scripts）**
   - script_id: 主键
   - name: 脚本名称
   - description: 描述
   - type: 脚本类型（Python、Shell等）
   - content: 脚本内容
   - version: 版本号
   - params_definition: 参数定义（JSON）
   - create_time: 创建时间
   - update_time: 更新时间

### 文件存储

1. **工作流定义文件**
   - 格式：JSON或YAML
   - 路径：/workflows/{workflow_id}/{version}.json

2. **脚本文件**
   - 格式：根据脚本类型（.py, .sh等）
   - 路径：/scripts/{script_id}/{version}.{ext}

3. **日志文件**
   - 格式：文本文件
   - 路径：/logs/{instance_id}/{node_instance_id}.log

## 错误处理

### 数据验证错误

1. **前端验证**
   - 输入字段格式验证
   - 工作流结构基本验证
   - 错误以表单提示方式展示给用户

2. **API服务验证**
   - 深度验证工作流定义的正确性
   - 验证参数的类型和取值范围
   - 返回结构化的错误信息给前端

### 执行错误

1. **节点执行错误**
   - 记录错误详情到节点实例
   - 根据工作流定义决定是否继续执行后续节点
   - 支持重试机制和错误跳过

2. **工作流执行错误**
   - 记录整体执行状态为失败
   - 保存已执行节点的结果
   - 提供恢复执行的能力

### 系统错误

1. **服务不可用**
   - 使用健康检查机制检测服务状态
   - 实现服务自动恢复
   - 提供服务降级策略

2. **数据不一致**
   - 使用事务确保数据操作的原子性
   - 定期执行数据一致性检查
   - 提供数据修复工具 